apiVersion: v1
kind: Service
metadata:
  name: {{ include "stepaway.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "stepaway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stepaway.selectorLabels" . | nindent 4 }}
